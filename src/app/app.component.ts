// This is the main component of the application. It controls the root view and any associated data.

@Component({
  selector: 'app-root',
  templateUrl: './app.component.html',
  styleUrls: ['./app.component.scss'],
})
// Define the AppComponent class.
export class AppComponent {
  // The title property holds the title of the application.
  title = 'boilerplate';
}
